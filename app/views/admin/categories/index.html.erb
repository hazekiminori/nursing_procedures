<div class="container">
  <h4>カテゴリー一覧</h4>
    <div>
　    <% @categories.each do |category| %>
        <%= link_to category.name, admin_category_path(category.id) %>
      <% end %>
    </div>
    <div>
      <%= link_to "カテゴリー追加する", new_admin_category_path %>
    </div>
</div>